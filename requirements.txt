# Core reasoning + knowledge system
chromadb==1.5.9
scikit-learn==1.8.0
networkx==3.6.1
numpy==2.4.4
requests==2.33.1

# Optional: real semantic embeddings (falls back to TF-IDF automatically
# if this is not installed or the model cannot be downloaded)
sentence-transformers==3.0.1

# API + demo
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5

# MCP server (pinned to the 1.x API used in this project)
mcp==1.30.0

# Submission doc generation
python-docx==1.2.0

# Testing
pytest==9.1.1
httpx==0.28.1
